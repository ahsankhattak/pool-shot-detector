# config.py

# Roughly outline YOUR playing table's felt area (not the whole frame)
# Pick 4+ points tracing the inner cushion edge, clockwise from top-left
TABLE_MASK_POLYGON = [
    (95, 320),
    (700, 295),
    (650, 1185),
    (10, 1230),
]